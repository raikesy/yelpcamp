<div class="container">
    <div class="card bg-dark text-white">
        <img src="{{image}}" alt="Campsite image" class="card-img">
        <div class="card-img-overlay">
            <h1>{{name}}</h1>      
            <p>{{description}}</p>
        </div>
    </div>
</div>